import { useState } from "react";
import { Warehouse } from "../models/product"; // Update your Product and Inventory types

export default function Warehouse() {

    const [warehouses, setWarehouses] = useState<Warehouse[]>([]);
    return (
        <div>
            

        </div>
    );
}